# .env is gitignored — never commit real API URLs/secrets here.

# Base URL of the Laravel API. During local development this might be
# something like http://localhost:8000/api once the backend is running.
VITE_API_BASE_URL=/api
